{
  "name": "shelfcheck",
  "version": "1.0.0",
  "private": true,
  "description": "Scan a food barcode and instantly see a health verdict, powered by Open Food Facts.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint js tests",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "check": "npm run lint && npm run typecheck && npm run test"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.8.0",
    "globals": "^17.9.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
